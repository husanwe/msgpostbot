services:
  msgpostbot:
    image: msgpostbot
    build:
      context: .
      dockerfile: Dockerfile.dev
    develop:
      watch:
        - path: ./package.json
          action: rebuild
        - path: ./package-lock.json
          action: rebuild
        - path: ./
          target: /app
          action: sync
